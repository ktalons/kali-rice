# Nothing from an actual engagement ever belongs in this repo.
# HTB permits publishing solutions only for Tier 0 modules, retired
# Machines/Challenges/Sherlocks, and Starting Point. This repo is public, so
# the safe surface is zero rather than small.
*.ovpn
htb/
boxes/
loot/
*.nmap
*.gnmap
*.xml
notes.md

# Local noise
.DS_Store
*.swp
*~
.rice-backup/
